# Arithmetic-Logic-Unit
RTL Verilog implementation of an Arithmetic Logic Unit (ALU). Handles add, subtract, multiply, divide, bitwise logic (AND/OR/XOR/NOT), shifts, and comparisons. Modular, synthesizable design with testbenches.
